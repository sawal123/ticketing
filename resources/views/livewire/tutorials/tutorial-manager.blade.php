<section class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-800"><h2 class="text-lg font-bold">Tutorial Interaktif</h2><div class="mt-4 space-y-3">@foreach($tutorials as $tutorial)<div class="rounded-xl bg-slate-50 p-4 dark:bg-slate-900"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="font-semibold">{{ $tutorial['name'] }}</p><p class="text-xs text-slate-500">{{ $tutorial['type'] }} · {{ $tutorial['status'] }}</p>@if(isset($tutorial['note']))<p class="mt-1 text-xs text-slate-500">{{ $tutorial['note'] }}</p>@endif</div><div class="flex gap-2">@if($tutorial['key'] !== 'dashboard.getting-started')<a href="{{ route($tutorial['route']) }}" class="text-sm font-semibold text-indigo-600">Buka</a>@endif<button wire:click="resetTutorial('{{ $tutorial['key'] }}')" class="text-sm font-semibold text-slate-600">{{ $tutorial['key'] === 'dashboard.getting-started' ? 'Tampilkan Lagi' : 'Reset' }}</button></div></div></div>@endforeach</div></section>
