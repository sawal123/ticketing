<div class="space-y-6"><div><h1 class="text-2xl font-bold text-slate-800 dark:text-white">Pusat Panduan</h1><p class="text-sm text-slate-500">Panduan ringkas fitur dashboard.</p></div><input wire:model.live.debounce.300ms="search" placeholder="Cari panduan..." class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-800"><nav class="flex flex-wrap gap-2">@foreach(array_keys($categories) as $category)<a href="#{{ \Illuminate\Support\Str::slug($category) }}" class="rounded-lg bg-slate-100 px-3 py-2 text-sm dark:bg-slate-800">{{ $category }}</a>@endforeach</nav>@php($found = false) @foreach($categories as $category => $articles) @if($articles) @php($found = true)<section id="{{ \Illuminate\Support\Str::slug($category) }}" class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-700 dark:bg-slate-800"><h2 class="text-lg font-bold">{{ $category }}</h2>@foreach($articles as $article)<details id="{{ $article['id'] }}" class="mt-3 rounded-xl bg-slate-50 p-4 dark:bg-slate-900"><summary class="cursor-pointer font-semibold">{{ $article['title'] }}</summary><p class="mt-2 text-sm text-slate-600 dark:text-slate-300">{{ $article['body'] }}</p></details>@endforeach</section>@endif @endforeach @if(!$found)<p class="text-slate-500">Panduan tidak ditemukan.</p>@endif</div>
